import { Text, View } from "react-native";

export default function Plan() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Plan screen (render plan JSON here)</Text>
    </View>
  );
}
