{
  "tasks": {
    "start": "deno run --allow-net --allow-env --allow-read --watch index.ts",
    "deploy": "supabase functions deploy plan"
  },
  "compilerOptions": {
    "allowJs": true,
    "strict": true
  }
}